# Round 1 Implementation Plan

Goal: deliver a self-contained teacher submission and a separate reproducible research repository.
Architecture: one numerical core, deterministic standalone build, real-framework adapter, chronological research tools.
Tech stack: Python >=3.11, NumPy, SciPy, scikit-learn; skfolio for integration.
Spec: docs/design.md (approved chat design).

## Global constraints

Class CVXPYPortfolio; fit(self,X,y=None); real BaseOptimization; finite numpy (N,) weights; no shorts; sum=1; no equal-weight fallback; 252 lookback; no I/O in submitted file.

## Tasks

- [x] 1. Write core contract tests first; run `python -m pytest tests/test_core.py` and observe missing implementation failure. Implement covariance, HRP, convex simplex optimization, validity and fallback; run until green.
- [x] 2. Write build/parity tests; deterministic builder must embed core byte-for-byte and preserve teacher originals. Generate submission adapter retaining base constructor.
- [x] 3. Write data split, wealth/metrics and missing-fold tests; implement chronological backtest and bounded parameter search with holdout guard.
- [ ] 4. Write real skfolio integration and CVXPY objective cross-check tests; do not stub dependencies. Run or mark dependency block with evidence.
- [x] 5. Add Chinese README, algorithm explanation, exact run commands, source references, private-repository publication script, and CI configuration.
- [ ] 6. Run full locally available verification, dependency-block report, synthetic diagnostic runs; record actual environment and file hashes; package local git repository and artifacts. Publish only if authenticated write capability exists.

Tests must distinguish synthetic engineering evidence from market performance evidence. No test or return result may be invented.

## Final execution status

Task 4: tests implemented, real skfolio/CVXPY execution blocked by missing packages and failed DNS.
Task 6: local verification/package completed; remote creation/push not possible with exposed read-only connector and absent CLI authorization. No complete-release claim.
